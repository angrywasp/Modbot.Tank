rem | exec so the build process occurs locally to the build script and doesn't break anything
exec("bsi", "External/Modbot/make.bs --run buildFramework()")

include "External/Modbot/make.bs"

includeDirectories = fmt("{0} -I {1}", includeDirectories, dir + "/External/ModBot/Src/lib")

buildProgram("tx")
